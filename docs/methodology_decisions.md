# Methodology Decisions and Issue Log

This file records pipeline issues found during pilot runs and the changes made
to reduce bias, improve ranking quality, and make later methodological reporting
auditable.

## Current Scope

- The first-pass cheap-model ensemble is for inexpensive recall into a
  shortlist, not final ordering.
- The second-pass GPT-5.4 ranker is a high-recall comparative ordering stage,
  not the final headline ranking.
- The accepted-50 reference now uses PDF-aware frontier judge cards from GPT-5.5
  xhigh, GPT-5.4 high, and Claude Opus 4.7 high. Each card sees the configured
  first-page PDF excerpt plus extracted main-paper text. A GPT-5.5 xhigh
  all-pairs tournament over the synthesis top 30 is the current gold ordering
  for top-rank evaluation.
- Retrieval is disabled in the implemented pipeline: no web search, file search,
  vector store, or external tool list is passed to the model calls.

## Issue Log

| Date | Stage | Issue | Evidence | Change |
| --- | --- | --- | --- | --- |
| 2026-05-14 | PDF parsing | Compact representation omitted experiments, baselines, and ablations. | Qwen criticized missing ablations in papers whose full extracted text contained ablation sections. | Added `scoring_repr.txt` and made first-pass scoring use main paper body rather than abstract/intro/conclusion only. |
| 2026-05-14 | Parser | Naive appendix stripping matched inline references such as `Appendix E.` and cut off main-paper ablations. | `Dqp6IMI3gQ` lost its ablation section in `scoring_repr.txt`. | Tightened appendix detection to require an explicit standalone appendix-like heading after a blank line. |
| 2026-05-14 | Qwen first pass | Absolute 1-10 Qwen scores compressed into 7-8 and produced weak ordering. | ICML 2025 20-paper pilot had min 7, median 8, max 8 for executive priority. | Treat Qwen individual scores as recall signals only; add a batch-ranking first pass with forced ranks/buckets. |
| 2026-05-14 | Qwen first pass | JSON output can be malformed or omit auxiliary narrative fields on long contexts. | Two 20-paper pilot rows returned malformed JSON; targeted retry succeeded. | Added row-local artifact persistence and `--paper-id` retry path; validation focuses on core numeric fields. |
| 2026-05-14 | GPT-5.4 second pass | Qwen top ordering did not match strong-model ranking. | GPT-5.4 moved ShieldAgent from Qwen rank 6 to rank 1 and Scalable Meta-Learning from Qwen rank 9 to rank 2. | Use GPT-5.4 batch ranking as the first reliable ordering stage; Qwen selects candidate pool. |
| 2026-05-15 | Representation | ICML papers have a fixed main-paper page limit, so text extraction should use that structural prior. | Full body extraction can include non-main-material when heading detection fails. | Add a configurable first-N-page main-paper extraction path, defaulting to 10 pages before references/appendix cleanup. |
| 2026-05-15 | Ranking methodology | Broad impact differs by contribution type; benchmarks, datasets, infrastructure, scientific tools, and algorithms should not be collapsed into one undifferentiated category. | GPT-5.4 noted benchmark-local and workflow-automation papers were overpromoted relative to broader scientific/ML importance. | Add primary contribution class and impact route fields; report both overall rankings and category-specific rankings. |
| 2026-05-15 | Multimodal review | Text-only extraction may miss figure/table content, visual evidence, and PDF layout. | Current GPT-5.4 pass sends only extracted text. | Document text-only limitation. Keep retrieval off; consider optional Responses API PDF/page-image pass for final human-facing shortlist. |
| 2026-05-15 | Qwen batch ranking | Forced-distribution batch ranking produced a useful score spread but one live run duplicated a paper id and omitted another. | Batch-10 run returned `GByP03IitA` twice and omitted `7QFmZ7i7sr`. | Add strict batch validation for exact paper-id and rank coverage; tighten prompt to list required candidate IDs. |
| 2026-05-15 | Qwen batch ranking | Qwen can alter paper-id capitalization in structured output. | Retry changed `w2QNIkcwWw` to `w2QNIKcwWw`. | Add narrow case-only canonicalization against the known candidate ID set; continue failing on true duplicate/missing/extra IDs. |
| 2026-05-15 | Qwen batch ranking | Qwen can make near-match paper-id typos beyond casing. | Batch-8 run changed `GKZySvM2t9` to `KGZySvM2t9`. | Add a narrow one-missing/one-extra edit-distance repair for random paper IDs; ambiguous or duplicate coverage still fails. |
| 2026-05-15 | Provider robustness | Long OpenRouter batch calls can terminate with partial HTTP reads. | Batch-10 retry ended with `IncompleteRead(8602 bytes read)`. | Treat `http.client.IncompleteRead` as retryable in the provider client. Prefer smaller Qwen batches if instability persists. |
| 2026-05-15 | Pipeline evaluation | Qwen batch rankings and GPT-5.4 ranking need objective evaluation against a stronger reference, not just qualitative inspection. | Qwen and GPT-5.4 disagreed substantially in the pilot; batch composition can suppress strong papers. | Add a reference-set protocol and `eval-ranking` harness for top-k recall, precision, rank correlation, and category-specific metrics. |
| 2026-05-15 | Batch construction | Contribution type should be known before batch ranking so comparisons are fair and batches can be stratified. | Benchmark/data, infrastructure, safety/eval, theory, scientific tool, and algorithm papers have different impact routes. | Add `classify-contributions`, a cheap compact-text classification stage for routing only, not quality judgment. |
| 2026-05-15 | Reference ranking | The all-50 GPT-5.4 reference pass produced a complete-looking JSON response but duplicated one paper id and omitted another. | `reference_gold_rank_v1` duplicated `3go0lhfxd0` and omitted `zFR5aWGaUs`; validator rejected the artifact. | Add `repair-reference`, which uses the invalid ranking plus omitted-paper text to produce a validated full ranking; keep the failed raw/parsed artifacts for audit. |
| 2026-05-15 | Evaluation metrics | Spearman correlation was invalid for candidate rankings that cover only a subset of the gold set. | Raw absolute-rank comparison yielded impossible Spearman values below -1 for 8-20-paper candidate files. | Compute Spearman over relative order within the shared paper set; top-k precision/recall remain unchanged. |
| 2026-05-15 | Evaluation metrics | Same-k recall is not enough for shortlist evaluation. | A candidate top-30 can preserve all gold top-10 papers even if its top-10 ordering is imperfect. | Add a recall matrix for gold top-N inside candidate top-M, plus gold-anchored class recall using gold contribution classes. |
| 2026-05-15 | Batch orchestration | A single long Qwen batch-suite process can stall on provider retries while completed batches are already useful. | The `rr8x3` run stalled after 20 batch directories; completed responses and parsed artifacts were recoverable. | Add `score-pass1-batches`, per-batch artifacts, partial recovery, and targeted retry with `score-pass1-batch`. |
| 2026-05-15 | Qwen first pass | One sequential batch partition had poor recall and near-zero rank correlation against the reference set. | Sequential batch-8 had Spearman -0.0129 and gold top-10 recall 0.2 at candidate top-10. | Prefer multiple class-informed shuffled partitions; aggregate repeated local rankings before shortlist construction. |
| 2026-05-15 | Shortlist construction | Global Qwen aggregation can miss class-specific leaders and one gold top-10 paper. | `P0wSGDoip1` ranked 33rd globally despite being gold rank 10. | Add class-balanced shortlist construction using the separate contribution classification pass; this recovered all gold top-10 papers in the top-30 candidate pool. |
| 2026-05-15 | GPT-5.4 second pass | The second-pass ranker could omit candidates because exact coverage was not validated. | Initial GPT-5.4 top-30 rerank returned 26 of 30 candidates while reporting `ok`. | Add exact `ranked_papers` coverage/rank validation to `rank-pass2` and tighten the prompt with required candidate IDs. |
| 2026-05-15 | Reference stability | GPT-5.4 rankings are not perfectly stable across all-50 and shortlist contexts. | The class-balanced GPT-5.4 rerank included all gold top-10 candidates but ranked `P0wSGDoip1` and `GdYg0Ohx0k` outside its top 10. | Treat `accepted_50_gold` as a reference, not ground truth; final pipeline should use multi-run or pairwise/tournament robustness for the final shortlist. |
| 2026-05-15 | Qwen batch prompt | Qwen sometimes cross-contaminated rationales across papers in the same batch. | One partition attributed diffusion/crystal-material details to `viXwXCkA7N` and privacy-certification details to `7J1kwZY72h`. | Bump first-pass batch prompt to `pass1_qwen_forced_batch_rank_v2`; add sealed-paper instructions and a `paper_isolation_check` field. |
| 2026-05-15 | Qwen batch prompt | Applied, scientific-tool, safety/eval, benchmark, and infrastructure papers can be suppressed relative to core algorithm papers despite strong impact routes. | Class-balanced top-30 was needed to recover `P0wSGDoip1`, which global Qwen aggregation placed outside the top 30. | Add explicit non-algorithm impact-route language, `category_standout`, `category_rank_within_batch`, and `why_not_just_incremental_or_domain_specific` fields. Category standouts can vote to advance to strong-model review. |
| 2026-05-15 | Cheap-model selection | The OpenRouter model catalog has changed since the initial plan, and `qwen/qwen3-32b` may no longer be the best cheap default. | OpenRouter lists newer long-context candidates including `qwen/qwen3-235b-a22b-2507`, Qwen3.6 variants, `deepseek/deepseek-v4-flash`, and `inclusionai/ring-2.6-1t`. | Treat `qwen/qwen3-32b` as a baseline, not the assumed best model. Run accepted-50 model probes before choosing the production cheap round. |
| 2026-05-15 | Provider settings | Some OpenRouter reasoning models can spend most of the output budget on reasoning and return truncated or non-string JSON content. | A `qwen/qwen3.6-35b-a3b` accepted-50 probe produced malformed JSON after 6,630 reasoning tokens in one batch and non-string message content in two later batches. | Default OpenRouter requests to `reasoning: {"effort": "none", "exclude": true}` unless `OPENROUTER_REASONING_EFFORT` overrides it. |
| 2026-05-15 | Cheap-model selection | Model choice should be based on accepted-50 recall, not catalog novelty. | One-partition v2 probes found: `qwen/qwen3-32b` top-10-in-top-30 recall 1.00, `qwen/qwen3-235b-a22b-2507` 0.80, and `qwen/qwen3.6-35b-a3b` with reasoning disabled 1.00. | Keep `qwen/qwen3-32b` as baseline and treat Qwen3.6-35B as the strongest replacement candidate. Do not switch to Qwen3-235B unless a multi-partition probe fixes its top-10 misses. |
| 2026-05-15 | First-pass ensemble | Multiple cheap models can add recall, but raw row counts would overweight whichever model has more partitions. | The accepted-50 ensemble used three v2 cheap probes and 150 rows; each paper could have different numbers of votes by model. | Make `build-shortlist` accept multiple `--scores` files and normalize aggregation by source model before computing ensemble priority. |
| 2026-05-15 | First-pass ensemble | A wider cheap shortlist improves downstream recall more than trying to trust cheap ordering. | Three-model ensemble top-45 captured all gold top-10, 19/20 gold top-20, and the top paper in every contribution class. | Use a larger cheap-stage semifinal pool for the strong model. On small gold-50 tests, top-45 is the diagnostic analogue of a generous production shortlist. |
| 2026-05-15 | Two-stage strong ranking | A top-25 final-stage handoff was too narrow. | Stage-1 GPT top-25 omitted three gold top-10 papers (`il3KRr4H9u`, `mEV0nvHcK3`, `P0wSGDoip1`) before final ranking. | Split the strong pass into semifinal and final stages, but widen the final-stage candidate pool to top-35 on the gold-50 diagnostic. |
| 2026-05-15 | Two-stage strong ranking | Long semifinal rankings can fail structurally if output caps are too low. | A 45-paper GPT semifinal with 9k max output tokens truncated JSON at rank 41. | Use a smaller semifinal text budget and a higher output cap; keep exact coverage validation and retry on duplicate/missing paper ids. |
| 2026-05-15 | Final-stage evaluation | The two-stage pipeline improves deep recall but still does not solve top-rank ordering. | Final top-35 had gold top-10 recall 1.00 in the candidate pool and gold top-20 recall 0.90, but same@10 recall was 0.60 and Spearman was 0.4742. | Treat the current two-stage pass as a high-recall finalist generator. Add a pairwise/tournament pass for final top-10/top-20 ordering before making headline claims. |
| 2026-05-16 | Reference ranking | The previous accepted-50 reference was a single GPT-5.4 text-only ranking, so it could miss visual/table evidence and overstate one judge's preferences. | The project goal requires PDF evidence; user explicitly requested GPT-5.5 xhigh, GPT-5.4 high, and Opus 4.7 review with first-10-page PDFs. | Add `rank-frontier-pdf-cards`: one resumable PDF-aware card per paper per judge, using first-10-page PDF excerpts plus extracted text. |
| 2026-05-16 | PDF upload robustness | Some first-10-page PDF excerpts were too large for reliable provider upload. | Image-heavy excerpts reached 20 MB; GPT routes produced connection resets on multi-MB base64/file uploads. | Compress oversized excerpts with Ghostscript before upload. The stable GPT reruns used `--pdf-optimize-threshold-bytes 1000000 --pdf-settings /screen`; text remains included for details. |
| 2026-05-16 | Provider robustness | OpenRouter and direct OpenAI can return transient invalid JSON, SSL, or connection reset failures on long multimodal calls. | Initial frontier card streams produced row-local JSON decode, SSL, and connection reset failures. | Treat invalid provider JSON, SSL errors, remote disconnects, and connection errors as retryable; persist row-local errors and rerun without redoing successful cards. |
| 2026-05-16 | Reference synthesis | Averaging independent card scores still compresses rankings and is not a true comparative gold ordering. | Three-card deterministic ensemble had many tied/near-tied mean scores and only Spearman 0.3412 against the old reference. | Add `rank-frontier-card-synthesis`, which reads all three PDF-aware cards and forces a full comparative 1..50 ranking. Use this synthesis to seed a pairwise tournament pool. |
| 2026-05-16 | Gold ranking | A single synthesis pass can preserve broad structure but still leave top-rank disagreements unresolved. | The PDF-card ensemble and synthesis were close but not identical; user requested a tournament-style frontier adjudication. | Add `rank-frontier-card-tournament`: GPT-5.5 xhigh compares all 435 pairs in the synthesis top 30 using the three PDF-aware cards, persists 18 resumable batches, and aggregates by win points. This is the current accepted-50 gold reference. |
| 2026-05-16 | Pipeline evaluation | The older reference was text-only and no longer suitable for judging current pipeline quality. | Against tournament gold, old text-only gold Spearman is 0.2875, while PDF synthesis Spearman is 0.9769. | Re-run prior pipeline metrics against `data/reference/icml_2025_accepted_50_frontier_pdf_tournament_gold.json`; report both same-k recall and gold-top-N-in-candidate-top-M recall. |
| 2026-05-16 | Finalist selection | The GPT-5.4 stage-2 top-35 cut was the main recall failure, not the cheap first pass. | Under tournament gold, the cheap ensemble top-45 captured 10/10 gold top-10 and 19/20 gold top-20, while stage-2 top-35 missed two gold top-10 papers. | Add `select-finalists`, a deterministic conservative selector from cheap ensemble plus strong semifinal ranks. Production should avoid aggressive strong-model narrowing before PDF-aware cards/tournament. |
| 2026-05-16 | Tournament cost | All-pairs is highest confidence but scales quadratically. | A legacy 30-paper simulation suggested Swiss plus a dense playoff could preserve most all-pairs ordering, while pure neighborhood comparisons were unreliable. The later position-bias audit invalidated reuse of its exact metrics. | Prefer Swiss exploration followed by a dense playoff when cost matters, but regenerate schedule metrics from each current all-pairs artifact with `simulate-hybrid-tournament`. Keep full all-pairs as the validation option for smaller final sets. |
| 2026-05-16 | Production configurability | The full run needs explicit knobs for each stage's advance rate rather than hard-coded gold-50 sizes. | The desired production pool sizes differ by total parsed-paper count and budget. | Add tunable CLI args: `build-shortlist --limit`, `rank-pass2 --limit`, `select-finalists --limit/--semifinal-top/--cheap-top/--min-per-class/--disagreement-saves`, `rank-frontier-pdf-cards --paper-list`, and `rank-frontier-card-tournament --strategy/--top-n/--swiss-rounds/--playoff-top-n`. |
| 2026-06-06 | Release readiness | ICML 2026 metadata is public before PDFs/reviews are uniformly public. | `release-status` found 6,636 accepted poster-paper metadata rows, 6,621 OpenReview forum URLs, 0 ICML `paper_pdf_url` values, OpenReview submission-note API access returning non-public status to guests, and no public rejected-note rows. | Add `release-status` to audit metadata/PDF/review readiness before full crawls. Treat PDF/review absence as release-state, not crawler failure. |
| 2026-06-06 | Metadata crawl | Crawling thousands of ICML detail pages is unnecessary for metadata. | The ICML virtual-site JSON feed exposes titles, abstracts through a companion abstract map, authors, decisions, sessions, topics, and OpenReview forum URLs in two requests. | Add `crawl-accepted-virtual-json` and make it the default metadata path. Keep detail-page crawling for smoke tests/debugging. |
| 2026-06-06 | Representation | ICML 2026 camera-ready main body is 9 pages, while original submissions are 8 pages. | Author instructions state accepted papers are allowed one additional page in the camera-ready version. | Change the default parser/PDF-card excerpt to 9 pages. Use `--main-paper-max-pages 8` and `--pdf-excerpt-pages 8` for original submitted versions. |
| 2026-06-06 | Cheap ensemble | The OpenRouter cheap-model catalog changed again; using one cheap family is unnecessary when the pass is cheap and recall-oriented. | Current low-cost candidates include Qwen3.7 Plus, NVIDIA Nemotron 3 Ultra, Gemini 3.1 Flash Lite, Step 3.7 Flash, and Ring 2.6 1T. | Add cheap-model presets and `score-pass1-ensemble`. Default `production_2026_v1` uses Qwen, Nemotron, and Gemini families; `broad_2026_probe` is available for accepted-50 validation. Aggregate ensemble signals are stored for later stages. |
| 2026-06-17 | Release readiness | ICML 2026 public metadata changed, but PDFs/reviews still are not publicly crawlable. | `crawl-accepted-virtual-json` found 6,628 poster-paper rows, 6,614 forum URLs, and 0 PDF URLs. Eight rows present on 2026-06-06 are no longer in the poster feed. A 10-paper OpenReview title-resolution probe resolved 0 PDFs, and a sample OpenReview PDF endpoint returned HTTP 403. | Refresh the accepted metadata snapshot, keep treating PDF/review absence as release state rather than crawler failure, and re-run `release-status` before attempting full PDF download. |
| 2026-06-27 | arXiv fallback | Many accepted ICML papers have public arXiv preprints before official PDFs are exposed, but arXiv versions are not canonical camera-ready PDFs. | A 3-paper ICML 2026 live probe resolved 3/3 exact arXiv matches, and a 1-paper PDF download produced a valid PDF. | Add `resolve-arxiv` as a separate enrichment path. Store matches under `extra.arxiv_*`, keep official `pdf_url/pdf_path` untouched, require high confidence for automatic use, and preserve candidate diagnostics for audit. |
| 2026-06-27 | arXiv crawl robustness | A larger live arXiv probe exposed rate-limit and resumability weaknesses. | The initial 100-paper probe hit HTTP 429 around row 38, and the old command buffered final outputs until process completion. | Reduce arXiv API fanout to exact-title plus one author-constrained fallback query, default to a 12-second arXiv delay with 30-second retry backoff, persist a `.partial.jsonl` journal so interrupted runs can resume without repeating successful rows, and record only executed query variants in row diagnostics. |
| 2026-06-27 | arXiv match recall | Strict title similarity can miss true arXiv matches when authors reorder the title around the same contribution. | In the 100-paper probe, `CodeChemist` had identical authors and abstract but a reordered title, producing a medium-confidence false negative. | Promote matches with strong author overlap, strong abstract overlap, and adequate title similarity to high confidence. Add `--retry-unresolved` for targeted reruns after resolver tuning, and only skip previous rows that actually have an arXiv resolution status. |
| 2026-06-27 | arXiv unresolved audit | “No candidates” under the fast resolver does not always mean absent from arXiv. | A slow deep search over the 38 unresolved probe rows recovered 5 clear high-confidence matches and found 4 additional probable older-title versions plus 2 possible related/prior versions; 27 remained likely absent after broad author/title search. | Add optional `resolve-arxiv --deep-search` for unresolved audits and targeted recovery. Keep it off by default because author-pair/all-field searches are slower and return noisy related-paper candidates. |
| 2026-06-27 | arXiv responsible querying | Reruns and retries should not repeatedly hit arXiv for identical queries. | The 100-paper and unresolved probes showed that cache-free retries create unnecessary API load and make runtime harder to reason about. | Add a persistent arXiv query cache, cache/network stats in run reports, `--max-network-queries` budgets, `--refresh-arxiv-cache`, and long-cooldown handling for residual HTTP 429 responses. Compact candidate diagnostics after retries to keep audit files current. |
| 2026-06-27 | arXiv pilot readiness | The arXiv fallback needs evidence that it can run politely, resume after budget stops, expose ambiguous matches, and produce parseable PDFs. | A 500-paper fast resolver pilot completed through three budgeted invocations with 0 missing statuses, 319 high-confidence matches, 181 unresolved rows, and no rate-limit cooldowns. The audit bucketed rows into 319 automatic matches, 176 likely absent/no-candidate, 3 probable prior-title, 1 possible related/prior, and 1 weak related hit. A 200-PDF high-confidence download batch had 200/200 successful downloads; parsing produced 196 ok, 4 needs-review, 0 failed. | Treat the arXiv fallback as operationally ready for controlled enrichment, but keep provenance explicit (`arxiv_preprint`) and preserve review queues for ambiguous/older-title cases. Use budgeted metadata chunks, summarized resume logs, downloaded-only parse manifests, and row-local parse diagnostics for the production run. |
| 2026-06-28 | Official PDF readiness | ICML still does not expose explicit PDF URLs in the virtual-site JSON, but OpenReview forum PDFs are now public and notes/reviews are visible. | `release-status` found 6,628 poster metadata rows, 6,613 forum URLs, 0 ICML JSON PDF URLs, successful OpenReview note/review access, rejected-note rows, and a successful PDF probe at `https://openreview.net/pdf?id=<forum_id>`. A 5-paper `resolve-openreview-pdfs --download-pdfs` probe resolved 5/5 through direct forum IDs with 0 unresolved and 0 download failures. | Make official OpenReview forum PDFs the primary PDF acquisition path. Use arXiv only as fallback for records without official forum/PDF coverage, and keep arXiv provenance separate from canonical `pdf_url/pdf_path`. |
| 2026-07-13 | Canonical presentation labels | ICML publishes oral talks as separate event rows linked to poster records, so counting both inflates the paper corpus and dropping oral rows loses a useful human-selection signal. | The refreshed feed has 6,796 event rows: 6,628 posters and 168 orals. Merging linked oral metadata into poster records yields 6,628 unique papers, 574 spotlights, and 168 oral presentations. | Keep poster rows as canonical papers; merge oral schedule/URL metadata and preserve independent `is_spotlight` and `is_oral` labels for later evaluation. |
| 2026-07-13 | Published score snapshot | Human-score comparisons need structured, reproducible fields without unnecessarily redistributing review text. | The latest successful OpenReview API v2 snapshot uses `details=directReplies` and contains official-review overall, soundness, and confidence values for 6,341 accepted papers, with per-review IDs and timestamps. All 6,341 rows join to the canonical corpus by forum ID. | Store structured published reviewer ratings and provenance only. Describe them as public reviewer ratings, not private AC scores or an exhaustive record of hidden decision deliberation. |
| 2026-07-13 | Award labels | Awards are sparse, high-value human outcomes and should come from an official, auditable source rather than inferred page styling. | The official ICML announcement names two Outstanding Papers, five Honorable Mentions, one Outstanding Position Paper, one position Honorable Mention, and one historical Test of Time paper. Nine current-year titles match canonical records exactly after normalization; the historical paper is intentionally unmatched. | Maintain a small source-linked awards JSON and exact-title enrichment report. Treat awards as evaluation labels, never model inputs during paper-only ranking. |
| 2026-07-13 | OpenReview access gate and resume policy | A bulk downloader must distinguish an endpoint-wide interactive gate from paper-specific missing files, and must retain progress across disconnects. | OpenReview's unauthenticated API/PDF endpoints returned an access-gate 403. The hardened resolver recovered five valid local official PDFs, issued one network request, persisted a 6,628-row snapshot plus row journal, and paused with 5 complete and 6,623 pending. | Pause immediately on access challenges; do not retry every paper. Validate PDF magic bytes, hash local files, resume completed rows, and restart the same conservative 2-second-delay command when access clears. |
| 2026-07-13 | Authenticated official PDFs and temporary fallback | OpenReview PDFs are online, but its documented attachment/PDF endpoints now require interactive verification or an authenticated API v2 session from the production host. This is an access-mode issue, not evidence that the files are absent. | Both `api2.openreview.net/pdf?id=<forum_id>` and `/attachment?id=<forum_id>&name=pdf` returned `ChallengeRequiredError`. ICML's 6,628-row JSON export contained no PDF fields. Meanwhile, a restartable arXiv batch verified 200 existing files and downloaded 100 new high-confidence matches at a five-second interval with 0 failures; all 300 passed PDF signature checks, and the new 100 parsed with 0 failures. | Configure authenticated OpenReview access for the definitive camera-ready crawl. Keep arXiv downloads explicitly labeled `arxiv_preprint`, use them as a temporary working corpus, and replace them before final ranking whenever the official copy is available. |
| 2026-07-13 | arXiv refresh and bounded expansion | Perfect downloads among already matched papers must not be confused with full-corpus arXiv coverage. Long refreshes also need to avoid re-querying the same unresolved rows after every request-budget stop. | The older 200 PDFs were re-downloaded with 0 failures and 0 checksum changes. The previous resolver baseline was 319/500 high-confidence matches. A 25-request refresh chunk retried 13 unresolved rows and found one new exact July upload (`63530`, arXiv `2607.04293`), producing partial coverage of 320/500 and 301 downloaded PDFs. A stable retry-campaign ID now marks rows attempted in each bounded campaign. | Report match coverage and conditional download success separately. Continue the refresh and 1,000-PDF expansion in request-budgeted, resumable campaigns with 12-second metadata and 5-second PDF delays; retain exact provenance and replace preprints with official copies before final scoring. |
| 2026-07-14 | Overlapping arXiv retry campaigns | Targeted network recovery and a bulk unresolved refresh can overlap. Recording only the latest campaign ID can make an already attempted row eligible again in the earlier campaign. | The first refresh chunk found exact match `60602` (arXiv `2606.31856`); two transient DNS failures were then retried as a targeted campaign and cleanly resolved to no candidates. Subsequent bounded chunks resumed at the correct rows and found `63617` (arXiv `2607.04643`, exact), `65080` (arXiv `2607.04112`, high confidence), `61052` (arXiv `2607.05536`, exact), `65607` (arXiv `2607.09450`, exact), `65636` (arXiv `2606.30523`, exact), `65345` (arXiv `2606.30966`, exact), and `64143` (arXiv `2606.27731`, exact). All eight new PDFs downloaded and parsed successfully, raising the pilot to 328/500 matches and 309 validated PDFs. | Preserve the complete set of attempted campaign IDs per paper across output snapshots and append-only journals. Continue bounded refreshes without repeating rows attempted by any overlapping campaign. |
| 2026-07-14 | Pilot completion and production expansion | Ordered download limits and corpus ordering are independent concerns: newly inserted matches can move the eligible boundary, and the production manifest is not ordered like the pilot source. Parsing a capped downloader's full output also incorrectly labels non-downloaded rows as missing PDFs. | The refresh finished at 329/500 matches, a gain of 10 over the 319-match baseline. Downloading through all 329 matches recovered 19 previously matched tail PDFs; these parsed as 17 `ok`, 2 `needs_review`, and 0 failed. Exact paper-ID exclusion produced a 6,128-row non-pilot expansion source. Its first 19 processed records yielded 13 exact matches; all PDFs validated and parsed as 12 `ok`, 1 `needs_review`, and 0 failed. | Set the cumulative download limit from the current match count, not the previous local count. Emit an explicit `--downloaded-only-out` manifest for parsing while retaining the full cumulative output for provenance and resume. Exclude pilot records from production by stable paper ID. |
| 2026-07-15 | Authenticated OpenReview PDF access | Official camera-ready PDFs should replace arXiv preprints, but authentication must not expose credentials or create aggressive account traffic. | A single API v2 login obtained a one-hour bearer token. A separate five-paper probe downloaded 5/5 official PDFs with a two-second delay, zero retries, no challenge, and no failures; the three papers also present in the arXiv corpus had different checksums from their preprints. Staged production checkpoints completed 25/25, 100/100, 498/500, 997/1,000, 1,496/1,500, 1,996/2,000, 2,496/2,500, 2,993/3,000, 3,493/3,500, 3,992/4,000, 4,492/4,500, 4,989/5,000, 5,487/5,500, 5,986/6,000, 6,486/6,500, and 6,614/6,628 selected papers without access signals. Two isolated read timeouts each succeeded on the first later invocation, demonstrating resumability and leaving zero outstanding failures; the final 628 papers downloaded without retry. All 6,614 available files passed signature, checksum, and provenance validation; parsing produced 6,611 `ok`, three `needs_review`, and zero failures. The fourteen unresolved records have no OpenReview forum ID, rather than failed PDF requests. | Make authenticated API v2 downloads the primary corpus path. Require explicit `--use-openreview-auth`, keep credentials only in the ignored owner-readable `.env`, never persist the bearer token, stop immediately on access challenges, advance in cumulative batches that fit within token lifetime, label official provenance explicitly, and retain arXiv only as fallback. |
| 2026-07-15 | Cheap-model refresh | New low-cost models should be added only when they improve shortlist recall, not because the catalog is newer or larger. | A 14-model accepted-50 screen produced 11 usable complete or near-complete rankings, including follow-up tests of Grok 4.5 low, Claude Sonnet 5 low, and Qwen3.7 Max. Across all 330 four-model subsets, the selected Nemotron 3 Ultra + Gemini 3.1 Flash Lite + GPT-5.6 Luna + Grok 4.3 panel retained 10/10 gold top-10 papers by rank 20, 18/20 gold top-20 by rank 30, and 24/30 gold top-30 by rank 30 at $0.784. The previous three-model panel retained 17/20 and 23/30 at rank 30; all 11 usable models retained 18/20 and 23/30 at about four times the cost. No equally strong four-model subset was cheaper or faster. Step 3.7 Flash and Gemini 3.5 Flash rejected reasoning-off requests, DeepSeek V4 Pro returned one malformed final batch, and DeepSeek V4 Flash high was stopped for impractical throughput. | Add `production_2026_v2` with the four measured families and make it the default. Retain v1 and probe presets for reproducibility. Keep the first-pass objective focused on recall; do not assume that adding models monotonically improves an averaged rank. |
| 2026-07-15 | Strong/frontier model refresh | The production judges should reflect current frontier capabilities while preserving cross-lab diversity and a legible three-judge design. | OpenAI's current family positions GPT-5.6 Terra for intelligence/cost balance, Luna for high volume, and Sol as the flagship. Anthropic Fable 5 is its strongest broadly available model but can route safeguarded requests to Opus. Gemini 3.1 Pro provides an independent third laboratory. One-paper PDF/JSON smoke cards completed 3/3 with no served-model substitution and cost $0.258, $0.608, and $0.065 respectively. | Use GPT-5.6 Terra high for the strong semifinal; use GPT-5.6 Sol xhigh, Claude Fable 5 high, and Gemini 3.1 Pro high for PDF cards; use GPT-5.6 Sol xhigh for synthesis and tournament. Store requested and served model IDs. Keep the GPT-5.5/GPT-5.4/Opus 4.7 accepted-50 gold unchanged as a historical evaluation artifact; the smoke validates interfaces and cost, not a new gold ranking. |
| 2026-07-15 | Provider failure hygiene | A deterministic model configuration, authentication, permission, or rate-limit error should not trigger hundreds of identical batch requests, and persisted provider errors should not contain account-scoped identifiers. | Two probe models returned HTTP 400 on every batch because reasoning was mandatory; OpenRouter error metadata also included a pseudonymous user ID. The production audit found the same stop policy was needed for HTTP 401, 403, and 429 responses. | Stop a model suite after the first request-wide HTTP 400, 401, 403, 404, 422, or exhausted 429 response; report skipped batches and the blocking reason, redact account identifiers from provider error details, and record the provider-returned served model for audit. |
| 2026-07-15 | Strong-semifinal diversity | A single strong text judge creates an avoidable model-family dependency, but using Opus across the full semifinal pool adds cost and overlaps with the Anthropic frontier judge. | Claude Sonnet 5 completed the accepted-50 probe reliably and is priced below Terra at current rates. The historical evaluation establishes the need for conservative handoff, but does not yet establish that a new dual-judge semifinal improves gold recall. | Run GPT-5.6 Terra high and Claude Sonnet 5 high independently over identical semifinal candidates. Aggregate equal-weight normalized ranks locally, retain source rows and model IDs, and preserve high judge-disagreement papers. Reserve Opus 4.8 for targeted adjudication or robustness checks. Compare Terra alone with the ensemble at an equal finalist budget before freezing production results. |
| 2026-07-25 | Pairwise aggregation | Raw win rates do not adjust sparse Swiss standings for opponent difficulty, while combining Swiss and playoff matches would mix two different schedules. Model-based intervals can also be mistaken for judge uncertainty. | On the 2025 dense 30-paper gold tournament, regularized Bradley-Terry converged and retained 9/10 of the direct-win top 10, with no paper moving more than two ranks. Dense all-pairs schedules already balance opponent strength; Swiss schedules do not. | Fit Bradley-Terry separately by stage: use it for opponent-adjusted Swiss advancement and report it as a playoff sensitivity analysis beside direct win rates. Require a positive regularizer, fail on non-convergence, and label observed-information intervals as conditional on the recorded judgments rather than uncertainty over prompts or model families. |
| 2026-07-25 | Human-comparison population | Human-outcome analyses can look valid while using a truncated shortlist or the most favorable repeated model score. Missing tier fields can also turn rejected or unlabeled records into posters. | A real-schema probe joined 45 aggregate rows to a 50-paper manifest without warning. Historical repeated first-pass rows included one paper scored 1, 9, and 7; selecting the maximum would not represent the ensemble. | Require one canonical aggregate row per paper, reject duplicates, report exact manifest coverage, and fail below a declared threshold. Keep accepted presentation tier, published reviewer scores, and accepted-versus-public-rejected status as separate human axes. |
| 2026-07-25 | Human-comparison inference | Comparing a full-corpus cheap ranking with a strong ranking only on selected finalists confounds judge quality with selection. Unthresholded galleries and failed taxonomy batches can also overstate divergence and reliability. | The initial comparison implementation counted every non-award poster as a gem and every honored paper as a blind spot; strong metrics changed both model and population; degenerate all-negative code labels returned kappa 1.0. | Compare cheap and strong ranks on the identical finalist subset, separately report advancement recall, use explicit rank-tail rules, and add bootstrap AUC intervals. Preserve taxonomy evidence and served-model metadata, code the full tail after freezing the codebook, report held-out summaries, exclude failed cases, and pair kappa with prevalence and raw agreement. |
| 2026-07-25 | Production preflight | Direct OpenAI Responses rejected the otherwise valid `seed` parameter, and model commands inherited crawler-oriented 30-second timeouts. | The six-paper live smoke failed before inference with HTTP 400 for `seed`; Terra then exceeded 30 seconds on valid reasoning requests. | Do not send `seed` to the OpenAI Responses endpoint. Keep it for compatible chat endpoints. Use 180-second cheap-model and 600-second strong/frontier defaults. |
| 2026-07-25 | Corpus-scale reliability | Contribution routing, cheap ranking, and strong semifinal ranking could lose completed work after interruption; the strong command also attempted an infeasible corpus-wide response. | Sonnet truncated a six-paper, 102k-input-token ranking at the former 6k output cap. A 1,000-paper semifinal cannot fit one prompt or one structured response. | Use fingerprinted, exact-coverage, resumable batches. Route in size-16 batches; rank cheap and strong stages in small class-stratified batches. Aggregate strong judgments by mean normalized local rank across two deterministic partitions before the Terra/Sonnet ensemble. Keep single-response `rank-pass2` only for small diagnostics. |
| 2026-07-25 | Artifact integrity | A semifinal JSON object written with a `.jsonl` suffix was parsed by extension, finalist fill reasons could be attached to already preserved rows, and aggregate provider cost dropped floating-point values. | These issues surfaced in the end-to-end smoke despite unit-level stage success. | Detect ranking serialization from content, skip already selected rows during fill, and aggregate numeric usage while excluding booleans and nested detail. |
| 2026-07-25 | Frontier fallback and accounting | Frontier providers can return billed responses with block-structured content, refusals, or structurally invalid JSON. Silent provider routing would also blur model identity. | In the 12-paper preflight, Sol and Gemini each needed one bounded retry. Fable failed twice on one paper; an explicit Opus 4.8 fallback succeeded immediately. | Accept documented text-block response shapes, preserve raw failed responses and usage, and use an explicit Fable-to-Opus fallback. Store the primary attempt, fallback reason, requested and served models, and cumulative attempt usage on the paper. |
| 2026-07-25 | Tournament position control | Pairwise adjudication can inherit the seed ranking if stronger seeds always occupy position A or if pair batches follow seed order. | The legacy 2025 tournament selected paper A in 378/435 matches. The first 12-paper preflight selected A in 66/66 matches and reproduced the synthesis order exactly. Randomizing A/B alone balanced positions but left seed-correlated batch composition. | Use `frontier_pdf_card_pairwise_tournament_v3`: deterministically randomize pair-to-batch assignment, A/B presentation, and evidence-block order. The seed may select the pool and break exact aggregate ties, but is not exposed as a rank. Treat earlier tournament artifacts as legacy and regenerate gold metrics. |
| 2026-07-25 | Medium production preflight | Unit tests and a six-paper smoke do not exercise realistic context lengths, provider variation, candidate preservation, or human-comparison joins. | A stratified 48-paper run completed every stage. The cheap top-32 retained 9/9 award papers; 36/36 frontier cards and 66/66 randomized matches completed; A/B wins were 32/34; card synthesis had Spearman 0.972 and top-5/top-10 recall 1.0 against the tournament. | Freeze the stage interfaces and launch defaults after the corrected historical gold rerun. Interpret the 48-paper figures as operational diagnostics, not population estimates, because selection deliberately oversampled human-honored papers. |
| 2026-07-25 | Corrected gold and tournament budget | Legacy gold metrics cannot justify a production schedule after the position-control prompt changed. | The randomized v3 gold completed 435/435 comparisons with 221 A wins and 214 B wins. PDF synthesis had Spearman 0.984 and 10/10 top-10 recall. The current four-model cheap ensemble retained every gold top-10 paper by rank 20 and 18/20 top-20 papers by rank 30; the old strong top-35 cut retained only 8/10 and 15/20 by rank 30. A replayed Swiss-10 plus top-20 playoff used 255/435 pairs, had Spearman 0.996, and retained 10/10 top-10 and 19/20 top-20 papers. | Freeze randomized v3 as the accepted-50 reference. Keep the cheap shortlist generous, do not use the old aggressive strong cut, and use Swiss exploration followed by a dense playoff for the production cost-control schedule. Retain all-pairs as a sensitivity option on smaller finalist sets. |
| 2026-07-25 | Cheap-model concurrency | Sequentially running independent cheap judges wastes wall time but shared mutable state could compromise resume behavior. | An eight-paper live probe ran the four production models concurrently in 51 seconds versus 111 seconds of summed model time. All 32 judgments completed, each model retained its own fingerprints and run directory, and reported usage totaled $0.12. | Use `score-pass1-ensemble --model-workers 4` in production. Parallelize only across model streams; keep each model's deterministic batches sequential and independently resumable. |
| 2026-07-25 | Frozen scoring population | Automatic parser warnings and missing official forum IDs should not silently determine the study population. | All 6,614 official PDFs have nonempty PDF, compact, scoring, and full representations. Three `needs_review` cases were false positives caused only by a missing Abstract heading; their abstracts and main bodies are intact. Three of the fourteen records without official PDFs have validated high-confidence arXiv fallbacks. The remaining eleven have no usable PDF. A full dry run selected 6,617 unique papers into 414 classification batches. | Freeze `data/metadata/icml_2026_scoring_manifest.jsonl` as the production input: 6,614 official PDFs plus three arXiv fallbacks, with reviewed parser overrides and original provenance retained. Report 6,617/6,628 coverage and list all eleven exclusions from `icml_2026_scoring_manifest.report.json`. |
| 2026-07-26 | Prelaunch blindness and credit resumption | Human outcomes must not influence the counterfactual ranking, and a depleted provider balance must not trigger thousands of doomed requests. | Classification, cheap batch, and PDF-card prompts were paper-only, but legacy single-paper and strong-ranking formatters still serialized `source` or `decision_label`. HTTP 402 was not classified as a provider-wide blocking error, and card/tournament loops did not halt on such errors. | Remove all human-outcome fields from model-facing formatters, add explicit no-human-outcome instructions and regression sentinels, and bump every affected prompt version. Keep human metadata in a separate canonical manifest and join it only after freezing the AI ranking. Treat HTTP 402 as blocking, halt card and tournament streams without invoking same-account fallbacks, and fingerprint frontier-card inputs so exact reruns reuse only compatible successes. |
| 2026-07-26 | Direct identity redaction | Prompt instructions alone do not remove prestige cues from camera-ready PDFs, but destructive edits to canonical evidence would weaken provenance. | A separate derivative stage passed 50/50 ICML 2025 papers and 103/103 production-layout ICML 2026 papers, including three layouts without an `Abstract` heading. It removed bylines, affiliations, emails, acknowledgements, mail links, and author/XML metadata. Twelve stale metadata names were retained as drift signals after the complete camera-ready author bands validated as removed. Two malformed source PDFs produced recorded repair diagnostics but valid derivatives. An unchanged rerun resumed 103/103 in under half a second. | Preserve canonical PDFs and text. Score only fingerprinted identity-redacted derivatives, validate residual known names/emails/metadata and a detected author band, and fail closed. State explicitly that titles, self-citations, project names, writing style, and model memory prevent guaranteed anonymity. |
| 2026-07-26 | Structural outcome isolation | Prompt-level omission is necessary but leaves future code changes able to read human outcomes from the same record. | The canonical scoring population contained reviewer summaries for 6,341 papers and human decision or presentation fields for all 6,617 papers. No current prompt consumed them, but they remained adjacent to model inputs. | Emit a minimal blinded manifest containing only paper ID, title, validated anonymized artifacts, checksums, and parse state. Remove authors, source labels, URLs, decisions, ratings, presentation tiers, awards, and all other metadata before any model call; join the canonical manifest only after the AI ranking is frozen. |
| 2026-07-26 | Production redaction gate | Small probes did not cover every camera-ready byline layout or noisy metadata value. | The full run was stopped after 972 papers: 968 passed and four failed closed. Visual review found one superscript-fragmented author block with visible name prefixes, one already-anonymous submission, one metadata/camera-ready middle-name mismatch, and one bogus `TT` author token that caused over-redaction. Later passes stopped at numeric footnote markers concatenated directly with URLs (paper 66430), metadata author “Min Li” falsely matching LaTeX `\min \limits` tokens (paper 65377), a URL split across three font spans (paper 63810), an OpenReview ASCII author name whose camera-ready byline used non-decomposing `ø` characters (paper 61271), an already-anonymous byline with an attached numeric affiliation marker (paper 63410), and a camera-ready author name containing an extracted Unicode spacing-diacritic character (paper 67092). No model calls had started. A layout-based title-to-abstract identity band, token-boundary-aware name anchors, explicit uncommon-Latin transliteration, spacing-diacritic normalization, footnote-tolerant anonymous-byline handling, reviewer/source-footer removal, short-uppercase-token filtering, footnote-tolerant scheme matching, and a span-aware URL fallback corrected these cases. Each revision was required to pass 50 ICML 2025 and 103 ICML 2026 real-PDF regressions before resuming. | Freeze a new derivative version after each behavioral change, preserve every stop as evidence that the gate is effective, and resume only from version-invalidated fingerprints. Continue to fail closed and visually inspect every anomalous layout before paid scoring. |
| 2026-07-26 | Redaction runtime | Layout extraction requested PyMuPDF's default dictionary flags, which serialize embedded figures even though only text blocks are used. | Stack sampling before the first v18 journal write showed time spent encoding image blocks. Disabling image payloads reduced the first production paper from more than 27 minutes to 1.43 seconds. Extracted text and rendered pixels were identical across all nine pages, and both real-PDF regression sets retained their exact pass and retention statistics. | Request text-only layout dictionaries. Keep figures in the derivative PDF itself, but never decode them merely to locate text. |
| 2026-07-26 | Title preservation audit | Identity normalization removed spaces before collapsing transliterated `ae`/`oe`/`ue` sequences. This created false cross-word matches such as “to Enable” and caused some second title lines to be included in the author band. Separate fixed minimum-length and line-gap rules misclassified a one-word final title line (“Data”) and a widely spaced continuation (paper 63191). | The independent full-corpus audit caught these issues by comparing source and derivative first-page titles. Normalizing each alphanumeric token before joining prevents transliteration across word boundaries; contiguous title geometry preserves short metadata continuations and camera-ready subtitles; a title-height-relative gap and font-height check preserve widely spaced title lines without absorbing paper numbers or smaller author lines. Version 23 passed 181 unit tests and real-PDF regressions on 50/50 ICML 2025 and 103/103 ICML 2026 papers with zero exclusions or source-present title losses. PDF and scoring-text retention minima/medians remained 0.9777/0.9872 and 0.9493/0.9786 for 2025, and 0.9673/0.9870 and 0.9283/0.9855 for 2026. | Treat title preservation as an explicit final-gate invariant whenever the metadata title is present in the source PDF; record metadata/PDF title drift separately rather than treating it as redaction loss. |
| 2026-07-27 | Text retention audit | The text affiliation-block heuristic used any nearby occurrence of “laboratory” as an affiliation cue. In paper 66692, a genuine footnote block was surrounded by scientific discussion of laboratory layouts, so 35 content lines were replaced and the compact representation retained only 47.2%. | The live v23 run was stopped at the 524-paper retention gate. Affiliation expansion now uses anchored organization-name forms and bounded marker blocks rather than generic scientific vocabulary. The affected paper retains 99.75% while removing all six footnote/affiliation lines; two other low-tail cases improved to 93.75% and 90.59%. Version 24 passed 182 unit tests and both 50-paper ICML 2025 and 103-paper ICML 2026 gates with zero exclusions or source-present title losses. Remaining compact-tail losses were attributable to deliberate acknowledgement and author-contribution removal. | Audit retention tails for every representation at production checkpoints, not only residual identities and aggregate medians. Treat unexplained localized content loss as blocking even when every identity-leak check passes. |
| 2026-07-27 | Final production anonymization gate | The v24 selective run stopped on paper 67080 because its metadata title and camera-ready title differed and its byline used a longer author name than OpenReview metadata. A blanket rerun would have rewritten thousands of already-valid derivatives. The first full v25 audit also treated malformed uppercase metadata placeholders (`TT`, `LIU`) as real authors and searched the entire first page for metadata titles, causing nine identity false positives and one false title loss from a repository slug. | Version 25 adds a pre-Abstract known-author/email fallback only when title anchoring fails. It passed 183 unit tests, 50/50 ICML 2025 papers, 103/103 ICML 2026 regression papers, and the real 67080 case. Production promoted 5,895 checksum-verified v24 rows, reused 654 older PDFs after independent validation, regenerated only 68 PDFs, and regenerated text for 722 incomplete rows. The final manifest contains 6,617/6,617 `ok` rows, 6,617 PDFs, and 19,851 text artifacts. The independent scan verified checksums, page counts, blind structure, fingerprints, links, metadata, residual identities, and retention. A corpus-wide correction scan applied the production-tested placeholder rule and restricted title evidence to the pre-Abstract title region: all 6,049 source-present metadata titles were retained; 568 metadata titles differ from the PDF; final errors were zero. An eight-paper deterministic/anomaly visual sample preserved titles and scientific content. | Reuse immutable derivatives only after checksum and clean-audit validation; append a current-version journal row rather than rewriting bytes. Filter implausible metadata identities consistently in production and audit code. Define title preservation against the title region, not incidental page text. Preserve the initial fail-closed audit and the differential correction audit alongside the final passing report. No paid ranking call may start before this gate passes. |
| 2026-07-27 | Wrapped-identity correction before paid ranking | A Stage 2 dry-run sample exposed a correspondence name split by PDF extraction as `Man-` / `asi Sharma`. The v25 validator checked names line by line, so it missed names split across lines and partial stems left before an already-redacted continuation. A stronger full-corpus scan found nine additional complete wrapped names in later PDF pages. | Version 26 matches canonical names across hyphenated adjacent lines, redacts each physical PDF line separately, and removes embedded or wrapped `Correspondence to:` contact lines. Only affected artifacts were rebuilt: 341 v26 records now pass focused PDF/text/checksum/structure validation, while 6,276 v25 records that passed the exhaustive audit remain byte-unchanged. All 6,617 latest journal rows are successful and no correspondence contact cue remains in any of the 19,851 model-facing text artifacts. | Always generate a no-cost prompt sample from the final manifest before paid scoring. Validate line-wrapped identities and partial contact-line stems, not only complete names on individual extraction lines. Preserve selective checkpoints and append structured journal rows so fixes remain resumable and do not rewrite the clean corpus. |
| 2026-07-27 | Extracted-text affiliation gate | The first paid Stage 2 sample showed that clean PDFs were insufficient: two-column extraction sometimes placed affiliation footers after the Abstract, outside the original front-matter cut. The first 48 cheap-stage batches cost $1.1134 before requests were stopped. Successive broad diagnostics found wrapped, accented, acronym-only, company-only, and employment-note variants; naive organization matching also risked deleting scientific uses of “laboratory” and equation text near footer markers. | v27-v33 use convergent bounded identity blocks, early single-digit footnote/layout evidence, title-cased affiliation fragments, and explicit wrapped employment/contribution-note detection. Each behavior has a scientific-content counterexample regression. Corrections were text-only, fingerprinted, journaled, and selective; all PDFs remain checksum-identical. The final manifest contains 6,617 successful rows and the full v33 audit reports zero errors. A separate structural residual scan contains only ten manually inspected equation/introduction cases. The early Stage 2 artifacts are preserved under `icml_2026_pass1_cheap_ensemble_pre_v27_affiliation_contaminated` and excluded. | Treat generated prompts, not only source artifacts, as the final blindness boundary. Stop on any sampled prestige cue; test both leak examples and over-redaction counterexamples; prefer layout invariants over institution lists; preserve invalidated paid outputs for cost/provenance accounting but restart the stage in a clean directory. |
| 2026-07-26 | Gemini Flash Lite split | A newer cheap model should replace an incumbent only for the task where it improves the gold-set objective. | On the matched accepted-50 ranking pass, Gemini 3.5 Flash Lite modestly improved individual and four-model top-tail ordering. With the other three streams fixed, ensemble Spearman increased 0.646 to 0.662, top-10 recall at rank 10 increased 6/10 to 7/10, and top-20 recall at rank 20 increased 15/20 to 16/20; both retained 10/10 by rank 20 and 18/20 by rank 30, while top-30 same-k recall decreased 23/30 to 22/30. Its $0.101 run cost $0.032 more than 3.1. On contribution routing, however, Gemini 3.1 achieved 0.80 accuracy and 0.764 macro-F1 versus 0.72 and 0.602. | Use Gemini 3.5 Flash Lite with minimal excluded reasoning in the cheap ranking ensemble; keep Gemini 3.1 Flash Lite for contribution routing. Preserve both model IDs and effective reasoning in run artifacts. |
| 2026-07-26 | Cheap-stage response recovery | One Gemini ranking emitted an invalid JSON backslash escape and one classification response mistyped a single opaque paper ID; both otherwise contained complete paid judgments. Recalling the provider would waste money and could change outputs. | A narrow invalid-escape repair parsed the saved response, and an unambiguous one-missing/one-extra ID edit-distance repair restored exact batch coverage. Both failed raw responses were recovered locally on the next identical invocation without a provider call. | Preserve raw responses before parsing, retry parsing saved fingerprint-matching responses locally, repair only invalid JSON escapes and one unambiguous near-ID pair, retain the original raw artifact, and make all broader coverage errors fail closed. |
| 2026-07-27 | Contribution-routing position control | The complete sequential routing pass showed a small association between input slot and primary class. In a preregistered-style audit over 24 evenly spaced batches, reversing all 384 papers produced 81.25% primary-label agreement (Cohen's kappa 0.73). Core/non-core flips favored the earlier slot 36 to 20, an estimated 4.17 percentage-point effect with exact two-sided p=0.044. | A fixed hash-randomized corpus order makes prompt slot independent of source ordering before the route labels are used to construct ranking batches. Preserve the sequential pass and reversal probe as sensitivity evidence. Treat this router as batching infrastructure, not the final category judgment; store the four-model cheap ensemble's class votes, agreement, routing label, and resolved ensemble class for later category analyses. |
| 2026-07-27 | Classification retry audit | Retrying a failed classification batch could overwrite its root response and batch state, obscuring the sequence of transient provider failures and undercounting attempts after a resume. | The initial full routing pass required isolated retries for truncated responses and three coverage errors; successful retry artifacts replaced some failed root files even though stale error files remained. | Before any new provider call, archive a matching failed batch's prompt, raw response, parsed payload, error, and state under `attempts/attempt_NNNN`, then record the provider attempt index in the new state. Exact reruns still reuse successful fingerprints and retry only failed or missing units. |
| 2026-07-27 | Corrected routing production gate | The hash-randomized Gemini 3.1 Flash Lite pass completed 6,617/6,617 papers with exact IDs, 414/414 complete root artifact sets, exact requested/served model identity, no human-outcome fields, and no live URLs. Two malformed batches required bounded recovery; one repeated deterministic 16-paper response was replaced by two validated 8-paper responses with composite provenance. Successful artifacts cost $2.7022 and archived paid failures cost $0.0240, for $2.7262 total. The corrected pass agreed with the sequential pass on 83.2% of papers (kappa 0.75). One sampled `other` label was weak, and several method/tool/application boundaries were genuinely ambiguous. | Pass Stage 1 for provisional batch routing. Keep the position probe, sequential pass, corrected pass, failed attempts, split recovery, and final audit. Do not interpret this single router as ground-truth taxonomy; use the stored diverse cheap-model vote consensus for downstream category reporting and retain named boundary cases for sensitivity analysis. |

## Contribution Classes

Each paper should receive one primary class and optional secondary classes:

- `core_ml_algorithm`: new modeling, optimization, inference, or learning method.
- `theory`: formal analysis, guarantees, or conceptual theory.
- `benchmark_dataset`: benchmark, dataset, protocol, or evaluation resource.
- `infrastructure_systems`: systems, compilers, training infrastructure, tooling.
- `scientific_modeling_tool`: ML method primarily enabling empirical science,
  simulation, measurement, or discovery.
- `safety_governance_eval`: safety, alignment, governance, policy, auditing, or
  eval work.
- `application_method`: domain-targeted method whose main claim is practical
  performance in a specific application area.
- `analysis_position`: empirical analysis, synthesis, interpretability, or
  position-style contribution.

Overall ranking should remain a single list for "best overall contributions,"
but reported analyses should also include category-specific leaderboards. This
is cleaner than forcing datasets, infrastructure, and scientific tools to
compete only as if they were new algorithms.

## Ranking Principle

First-pass models should answer: "Is this worth sending to the expensive
strong-model/human review stage?" They should not be trusted to identify the
final best papers by absolute 1-10 scores.

Second-pass models should answer: "Among shortlisted papers, which have the
strongest evidence-grounded case for broad scientific or ML-field impact?"

## Evaluation Principle

Do not judge the cheap first-pass stage by whether its local ranks look
reasonable in isolation. Judge it by recall: does it preserve the papers that a
stronger reference judge would rank highly? This is especially important for
batch ranking because local competition can suppress strong papers if too few
papers advance from each batch.

For subset candidate files, rank correlation should be interpreted only as
relative ordering among shared papers. Top-k recall is the primary metric for
whether a cheap stage is good enough to feed the expensive stage.
